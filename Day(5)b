Write a C Program to determine the factorial of a number.

#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

int a,b=1;
scanf("%d",&a);
if(a<0)
    printf("ERROR");
else
{for(int i=1;i<=a;i++)
    b=i*b;
printf("%d",b);}

    
return 0;
}
